import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gracefuldeerskip',
  appName: 'graceful-deer-skip',
  webDir: 'dist'
};

export default config;
