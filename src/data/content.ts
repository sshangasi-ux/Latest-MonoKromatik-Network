// src/data/content.ts

// All content is now fetched dynamically from Supabase.
// These arrays are kept for reference if needed, but are no longer actively used.
export const videoSpotlights: any[] = [];
export const recentArticles: any[] = [];
export const upcomingEvents: any[] = [];